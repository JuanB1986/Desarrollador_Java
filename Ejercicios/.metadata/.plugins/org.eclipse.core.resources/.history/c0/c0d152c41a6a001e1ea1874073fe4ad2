package com.Ejercicios_Clase7;

public class  Carrito {
	private final int MaximoItems=10;
	private String fecha;
	private itemCarrito []items;
	private static int contadorItems=0;		
	private Descuento descuento;
	
	//Constructor
	public Carrito(String fecha, Descuento descuento) {
		this.fecha=fecha;
		this.descuento=descuento;
		this.items=new itemCarrito[this.MaximoItems];
	}
	
	//Metodo agregar items
	public void agregarItem(itemCarrito item) {
		
		if (this.contadorItems < this.MaximoItems)
			{
				this.items[contadorItems]=item;
			}
		else
		{
			System.out.println("Máximo de items alcanzado");
		}
	}
	
	//Metodo computo precio total
	public float precioTotal() {
		int i;
		float retval=0;
		
		for (i=0;i<this.contadorItems;++i) {
			retval += this.items[this.contadorItems].getPrecio();
		}		
		retval -= retval*this.descuento.getDescuento()/100;
		
		return retval;
	}	
	
	//Metodo computo peso total
	public float pesoTotal() {
		int i;
		float retval=0;
		
		for (i=0;i<this.contadorItems;++i) {
			retval += this.items[this.contadorItems].getPeso();
		}		
		return retval;
	}
	
	
	public void printTipoDescuento() {
		
		System.out.println(this.descuento.getClass().getSimpleName() + ": " + this.descuento.getDescuento() + " %");
		
	}
	
}
